version: '3.3'

services:
  rancher:
    container_name: rancher
    image: rancher/rancher:v2.2.4-rc20
    restart: on-failure
    ports:
      - 80:80
      - 443:443
    volumes:
      - /opt/rancher:/var/lib/rancher
#    networks:
#      - web
#      - internal
#networks:
#  internal:
#    external: false
#  web:
#    external: true
